# Astro ACG 知识库 - 验证清单 (checklist.md)

- [ ] Checkpoint 1 (Task 1): `package.json` 存在且包含 `astro`, `@astrojs/mdx` 依赖；scripts 中含 `dev`, `build`, `preview`, `postbuild`。
- [ ] Checkpoint 2 (Task 1): `astro.config.mjs` 存在且配置 `site` / `base=/Knowledge-based_ACG_Astro_Wiki/`；`integrations` 含 `mdx()`。
- [ ] Checkpoint 3 (Task 1): `npm install && npm run build` 成功，`dist/index.html` 生成。
- [ ] Checkpoint 4 (Task 2): `src/content/config.ts` 定义 Zod Schema，含 `title`, `order`, `layoutType`, `themePalette`, `isDraft`。
- [ ] Checkpoint 5 (Task 2): `src/content/` 下至少存在 `dr-stone/` 与 `maoyuu/` 两个作品集合，各含 2+ 章节 MDX。
- [ ] Checkpoint 6 (Task 2): 故意缺少 `title`/`order` 的章节会导致构建失败（Schema fail-fast）。
- [ ] Checkpoint 7 (Task 2): 构建后 `isDraft: true` 的章节不出现在产品站中。
- [ ] Checkpoint 8 (Task 3): `src/pages/[work]/index.astro` 与 `src/pages/[work]/[...slug].astro` 存在。
- [ ] Checkpoint 9 (Task 3): `dist/` 下各作品都存在 `index.html` 与各章节 HTML；`dist/404.html` 存在。
- [ ] Checkpoint 10 (Task 4): `src/styles/theme.css` 定义 `--bg-color`, `--text-main`, `--accent-primary` 等变量，并提供 `.theme-ocean/.theme-crimson/.theme-amber` 三套类。
- [ ] Checkpoint 11 (Task 5): `src/layouts/BaseLayout.astro`, `DocLayout.astro`, `TimelineLayout.astro`, `GalleryLayout.astro` 均存在；`DocLayout` 渲染三列结构。
- [ ] Checkpoint 12 (Task 5): 不同章节页面根据 `layoutType` 渲染对应布局。
- [ ] Checkpoint 13 (Task 6): `BaseLayout.astro` 中引用了 `<ViewTransitions />`；跨章节点击不白屏。
- [ ] Checkpoint 14 (Task 7): `src/utils/toc.ts` 能生成 `h2/h3/h4` 层级；`src/components/TableOfContents.astro` 渲染带锚点链接的目录。
- [ ] Checkpoint 15 (Task 7): TOC Island 使用 `IntersectionObserver`；滚动时当前高亮切换。
- [ ] Checkpoint 16 (Task 7): TOC 点击平滑滚动；`h3/h4` 手风琴折叠/展开。
- [ ] Checkpoint 17 (Task 7): 窄屏 TOC 以汉堡按钮+抽屉出现；桌面端固定侧栏。
- [ ] Checkpoint 18 (Task 7): View Transitions 切换后 TOC 的 `IntersectionObserver` 正确重建，无内存泄漏警告。
- [ ] Checkpoint 19 (Task 8): `src/components/WorkNavigator.astro` 存在且渲染全部作品。
- [ ] Checkpoint 20 (Task 8): `src/components/Pagination.astro` 根据 `order` 生成"上一章/下一章"；首章无上一章；尾章无下一章。
- [ ] Checkpoint 21 (Task 9): `src/components/MdxImage.astro` 存在；渲染的 `<img>` 含 `width/height`, `loading="lazy"`, `decoding="async"`；`<picture>` 含 AVIF/WebP。
- [ ] Checkpoint 22 (Task 10): `src/components/LazySection.astro` 使用 `client:visible` 懒挂载；示例章节演示效果。
- [ ] Checkpoint 23 (Task 11): `postbuild` 执行后 `dist/pagefind/` 存在；`SearchModal.astro` 与 search.ts Island 存在。
- [ ] Checkpoint 24 (Task 11): 搜索结果 URL 前缀为 `${BASE_URL}`，不因 base 路径丢失产生 404。
- [ ] Checkpoint 25 (Task 12): `.github/workflows/deploy.yml` 存在；使用 `actions/upload-pages-artifact` 与 `actions/deploy-pages`；`on: push: branches: [main]`。
- [ ] Checkpoint 26 (Task 12): `astro.config.mjs` 读取 `process.env.BASE_PATH` 或环境变量设置 `base`。
- [ ] Checkpoint 27 (Task 14): `src/pages/index.astro` 首屏含 Hero 区与亮点展示；滚动后出现作品列表区，含响应式作品卡片网格。
- [ ] Checkpoint 28 (Task 15): `src/components/mdx/` 下至少 10 个组件（`Callout, HeroCard, Card, ImageGallery, TimelineNode, InfoGrid, Quote, Divider, TagList, Aside`）均存在；配置中存在 MDXComponents 映射。
- [ ] Checkpoint 29 (Task 13): `README.md` 存在，含简介、徽章、`npm install && npm run dev`、目录结构、5 分钟作者指南。
- [ ] Checkpoint 30 (Task 13): `docs/code-wiki.md` 存在，含架构分层、Schema 字段表、≥10 个 MDX 组件 API、状态与生命周期、部署排错指南。
- [ ] Checkpoint 31 (性能 NFR-1): 首屏图片带显式 `width/height`；无明显布局偏移（CLS≈0）。
- [ ] Checkpoint 32 (可访问性 NFR-2): TOC 与 WorkNavigator 支持键盘 Tab/Enter；主色与背景色对比度达标。
- [ ] Checkpoint 33 (可维护性 NFR-3): 新增作品仅需在 `src/content/<new-work>/` 新增 MDX 集合 + Frontmatter，无需改动组件与路由源码。
- [ ] Checkpoint 34 (构建规范 NFR-4): `npm run build` 在 CI 环境零错误通过。
